To use this scraper, you'll need pipenv installed:
	apt-get install pipenv

Then, run pipenv's install command to initialize what's in the Pipfile:
	pipenv install

Finally, run the scraper using the helper script:
	./runScraper.sh
	
Results will be placed in the output folder.
